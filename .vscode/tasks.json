{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Main: Build",
            "type": "shell",
            "command": [
                "cd \"${workspaceFolder}/build\" && mingw32-make"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}